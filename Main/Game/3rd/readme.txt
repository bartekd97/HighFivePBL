For Debug build use static .lib libraries
For Release build use dynamic .dll libraries
After adding those dependencies don't forget to configure them for Debug and Release build separately and modifying releasecopy.bat script if needed.